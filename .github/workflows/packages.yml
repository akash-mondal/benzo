name: packages

on:
  push:
    paths:
      - "packages/**"
      - "apps/**"
      - "circuits/**"
      - "scripts/**"
      - "package.json"
      - "pnpm-lock.yaml"
      - "pnpm-workspace.yaml"
      - "turbo.json"
      - ".github/workflows/packages.yml"
  pull_request:
    paths:
      - "packages/**"
      - "apps/**"
      - "circuits/**"
      - "scripts/**"
      - "package.json"
      - "pnpm-lock.yaml"
      - "pnpm-workspace.yaml"

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: pnpm/action-setup@v4
        with:
          version: 10
      - uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: pnpm
      - run: pnpm install --frozen-lockfile
      - name: Lint
        run: pnpm lint
      - name: Production environment guard
        run: pnpm audit:prod-env
      - name: Check wallet proving artifact availability
        id: wallet_artifacts
        env:
          BENZO_ARTIFACTS_BASE_URL: ${{ vars.BENZO_ARTIFACTS_BASE_URL || secrets.BENZO_ARTIFACTS_BASE_URL }}
        run: |
          if [ -n "$BENZO_ARTIFACTS_BASE_URL" ]; then
            echo "available=true" >> "$GITHUB_OUTPUT"
          else
            echo "available=false" >> "$GITHUB_OUTPUT"
            echo "BENZO_ARTIFACTS_BASE_URL is not set; skipping wallet static build."
          fi
      - name: Build packages
        run: pnpm -r --filter '!@benzo/wallet-app' build
      - name: Typecheck hosted apps
        run: |
          pnpm --filter @benzo/console typecheck
          pnpm --filter @benzo/wallet-app typecheck
      - name: No client-side private audit store
        run: |
          if git grep -nE 'benzo\.console\.privateEvents|recordConsolePrivateEvent|clientAudit(Packet|OrgHash|PacketHash)' -- apps/console/src ':!apps/console/src/screens/AuditLog.test.tsx'; then
            echo "::error::private audit packets must come from the encrypted hosted tenant store, not browser localStorage"; exit 1
          fi
      - name: No client-side invoice import store
        run: |
          if git grep -n 'benzo.console.localInvoices' -- apps/console/src ':!apps/console/src/screens/Invoices.test.tsx'; then
            echo "::error::invoice handoffs must persist through the encrypted hosted tenant store, not browser localStorage"; exit 1
          fi
      - name: No client-side console workspace store
        run: |
          if git grep -nE 'localConsoleState|benzo\.console\.state\.encrypted|benzo\.console\.state\.key' -- apps/console/src; then
            echo "::error::console workspace state must come from the encrypted hosted tenant store, not a browser fallback store"; exit 1
          fi
      - name: No direct hosted wallet money writes
        run: |
          if git grep -nE "fetch\(\s*['\"]/?api/(send|relay/submit)" -- apps/wallet/src; then
            echo "::error::wallet money writes must go through the authenticated RPC gateway with idempotency"; exit 1
          fi
      - name: No fake fiat payout copy in wallet
        run: |
          if git grep -nE 'sent to your bank|arriving in your bank|MoneyGram-style|On its way' -- apps/wallet/src ':!apps/wallet/src/screens/TxDetail.test.tsx'; then
            echo "::error::wallet cash-out UI must describe the testnet reserve, not a real fiat payout"; exit 1
          fi
      - name: Build wallet app with browser proving artifacts
        if: steps.wallet_artifacts.outputs.available == 'true'
        env:
          BENZO_ARTIFACTS_BASE_URL: ${{ vars.BENZO_ARTIFACTS_BASE_URL || secrets.BENZO_ARTIFACTS_BASE_URL }}
          BENZO_ARTIFACTS_TOKEN: ${{ secrets.BENZO_ARTIFACTS_TOKEN || github.token }}
        run: pnpm --filter @benzo/wallet-app build
      # Poseidon2 constant-parity guard: the circom <-> TS <-> host hash must
      # never drift (a one-byte change would silently produce commitments the
      # chain can never match — a fund-loss class). Regenerate the params from
      # the committed circom source and fail if either the generated file or the
      # hand-synced @benzo/core copy has drifted. The circom-witness KAT lives in
      # circuits.test.ts and self-skips here where the circom artifacts are absent.
      - name: Poseidon2 parameter parity guard
        run: |
          node scripts/extract-poseidon-params.mjs
          git diff --exit-code -- circuits/poseidon_params/poseidon2_bn254.json
          cmp circuits/poseidon_params/poseidon2_bn254.json packages/core/src/crypto/poseidon2_bn254.json
      # Heavy snarkjs proving tests (circuits.test.ts) self-skip when the
      # gitignored .zkey/.wasm artifacts are absent; everything else — incl.
      # the committed VK/proof parity fixtures — runs and enforces invariants.
      - name: Test
        run: pnpm -r test
      # Honesty guard: no committed file may reference a dead pre-redeploy cluster
      # address (these once lurked in demo docs and sent reviewers to abandoned
      # contracts). The live IDs live only in deployments/testnet.json.
      - name: No stale-cluster addresses
        run: |
          if git grep -nE 'CCWBNQCJ|CAQZCOJU|CCSZAPFM' -- . ':!deployments/*.bak' ':!.github/workflows/packages.yml'; then
            echo "::error::a committed file references a dead pre-redeploy cluster address"; exit 1
          fi

  # Real ZK gate: fetches the exact published proving artifacts and runs the
  # snarkjs proving + on-chain-parity tests for real, so "green" actually means
  # proofs were generated and verified — not silently skipped. Opt-in: runs only
  # where the artifacts are reachable (BENZO_ARTIFACTS_BASE_URL secret set), so a
  # fork without the Release isn't a false red.
  zk-proofs:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: pnpm/action-setup@v4
        with:
          version: 10
      - uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: pnpm
      - run: pnpm install --frozen-lockfile
      - name: Check proving artifact availability
        id: artifacts
        env:
          BENZO_ARTIFACTS_BASE_URL: ${{ vars.BENZO_ARTIFACTS_BASE_URL || secrets.BENZO_ARTIFACTS_BASE_URL }}
        run: |
          if [ -n "$BENZO_ARTIFACTS_BASE_URL" ]; then
            echo "available=true" >> "$GITHUB_OUTPUT"
          else
            echo "available=false" >> "$GITHUB_OUTPUT"
            echo "BENZO_ARTIFACTS_BASE_URL is not set; skipping real proving gate."
          fi
      - name: Fetch proving artifacts (exact, sha256-verified against the manifest)
        if: steps.artifacts.outputs.available == 'true'
        env:
          BENZO_ARTIFACTS_BASE_URL: ${{ vars.BENZO_ARTIFACTS_BASE_URL || secrets.BENZO_ARTIFACTS_BASE_URL }}
        run: bash scripts/fetch-artifacts.sh
      - name: Real snarkjs proving + parity tests
        if: steps.artifacts.outputs.available == 'true'
        run: pnpm --filter @benzo/core test
