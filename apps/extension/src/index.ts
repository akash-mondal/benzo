/** @benzo/app-extension — Browser extension (scaffold). See README.md for the build-out plan. */
export * from "./platform.js";
import { ExtensionPlatform } from "./platform.js";
export const platform = new ExtensionPlatform();
