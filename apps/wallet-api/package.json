{
  "name": "@benzo/wallet-api",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "main": "./dist/server.js",
  "description": "BFF for the Benzo consumer wallet — real on-chain shielded USDC via @benzo/core (testnet).",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "serve": "node dist/server.js",
    "dev": "tsc -p tsconfig.json && node dist/server.js",
    "test": "vitest run --passWithNoTests"
  },
  "dependencies": {
    "@benzo/core": "workspace:*",
    "@benzo/links": "workspace:*",
    "@benzo/types": "workspace:*",
    "@neondatabase/serverless": "^1.1.0",
    "@stellar/stellar-sdk": "^15.1.0"
  },
  "devDependencies": {
    "@types/node": "^20.14.0",
    "typescript": "^5.5.0",
    "vitest": "^2.0.0"
  }
}
