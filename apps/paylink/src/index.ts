/** @benzo/app-paylink — Payment-link microsite (scaffold). See README.md for the build-out plan. */
export * from "./platform.js";
import { PaylinkPlatform } from "./platform.js";
export const platform = new PaylinkPlatform();
